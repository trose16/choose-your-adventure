<% if user_signed_in? %>
  <%= "#{@user.username}!" %><br>
  <%= link_to 'Sign out', destroy_user_session_path, method: :delete %>
<% else %>
  <h1>Please, Sign up to play!</h1>
  <%= link_to 'Sign up',  new_user_registration_path  %><br>
  <%= link_to 'Sign in', new_user_session_path %>
<% end %>
